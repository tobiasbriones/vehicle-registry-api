# Database Notes

## Vehicle Log

The `vehicle_log` table can still validate `mileage` to be increasing, but
that'd make the design much more complicated. Increasing mileage can be
validated in the app layer, as well.
