@host = http://localhost:3000

### GET request to server root
GET {{host}}/

### Create Vehicle
POST {{host}}/vehicles
Content-Type: application/json

{
    "brand": "content",
    "model": "content",
    "number": "content"
}
